name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
      - name: Test
        run: ./scripts/swiftpm.sh test
      - name: Build release
        run: ./scripts/swiftpm.sh build -c release
